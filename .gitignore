reports/
.DS_Store
*.bak

# Исходный скрипт заливки из helpdesk-tracker: он специфичен для того проекта
# (имена образов, адрес лабораторного DefectDojo) и целиком заменён на
# ci/dd-push.sh. Лежит локально как справка; git add -f, если нужен в истории.
/dd-push.sh
